# Auth Core

Reserved for authentication/session helpers and the future administrative access flow.

G2 does not import Firebase Auth or admin-gate logic yet. Authorization must later be enforced by both the application flow and the backend/Firebase security rules; this directory must never be treated as the sole security boundary.
