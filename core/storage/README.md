# Storage Core

Reserved for future file and media upload abstractions.

The LaR site does not connect uploads during G2. When storage is introduced, file type/size validation, authorization and backend security rules must be migrated together with the tested infrastructure.
